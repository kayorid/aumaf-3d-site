# URL da API backend — usada no formulário de contato (client-side)
PUBLIC_API_URL=http://localhost:3000

# Instagram feed via Behold.so — apenas o FEED_ID público (UUID).
# Sem este valor a seção de Instagram some silenciosamente do site.
# Cadastrar a conta @aumaf3d em https://behold.so e copiar o ID do feed gerado.
PUBLIC_BEHOLD_FEED_ID=
