# URL da API backend — usada no formulário de contato (client-side)
PUBLIC_API_URL=http://localhost:3000

# IndexNow — mesma chave usada em INDEXNOW_KEY do backend.
# Expõe /<KEY>.txt para verificação de propriedade junto a Bing/Yandex/etc.
# PUBLIC_INDEXNOW_KEY=
